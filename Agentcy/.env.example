# Azure Foundry / Azure AI Projects connection
# Fill these in locally (do NOT commit secrets), then `source .env` or export them.

# Foundry project endpoint (example pattern):
# https://<your-resource>.services.ai.azure.com/api/projects/<your-project>
USER_ENDPOINT=

# Agent + model
AGENT_NAME=story-agent
MODEL_DEPLOYMENT_NAME=

# Alternative auth (Project API key). Leave empty if using DefaultAzureCredential.
# Never commit real keys.
PROJECT_API_KEY=

# Feedback storage
# If set, the feedback API will create GitHub Issues server-side (so players never touch GitHub).
GITHUB_ISSUES_REPO=

# Token used by the API to create issues (cloud persistence).
# Fine-grained PAT recommended with Issues: Read/Write on the target repo.
GITHUB_TOKEN=
//step to get hte igthub Token

# Where to append feedback records as JSONL (useful for testing). Beware: many hosts have ephemeral disks.
FEEDBACK_STORE_PATH=data/feedback.jsonl

# Auth (Service Principal example)
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_SECRET=
