Thermal Drift — Prototype

Controls
- Arrow Up: accelerate
- Arrow Left / Right: steer
- R: restart

Description
A small top-down prototype that demonstrates a 'frost build' mechanic: the faster you drive, the more frost and particle residue accumulates on the scene. Built as a minimal playable prototype for iteration.

To run
Open `Projects/games/thermal-drift/index.html` in a browser (or use Live Server / `python -m http.server` from the repo root).